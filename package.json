{
  "name": "simply-portraits",
  "version": "1.2.2",
  "description": "A bare bones module for adding portraits to chat messages.",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --minify --sourcemap --outdir=build && esbuild src/main.css --bundle --outdir=build && cp src/module.json build/module.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Vito Gamberini",
  "license": "CC0-1.0",
  "devDependencies": {
    "@league-of-foundry-developers/foundry-vtt-types": "^9.280.0"
  },
  "dependencies": {
    "esbuild": "^0.15.12"
  }
}
